# ============================================================================
# Instagram Follow Automation Service - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control - it contains sensitive credentials
# ============================================================================

# Server Configuration
# Port the HTTP server will listen on (default: 3000)
PORT=3000

# Node environment (development, production, etc.)
NODE_ENV=production

# Security: Bearer Token for API Authentication
# This token must be included in the Authorization header of all requests
# Format: Authorization: Bearer YOUR_TOKEN_HERE
# CRITICAL: Use a strong, randomly-generated token (minimum 32 characters)
# Example: openssl rand -hex 32
BEARER_TOKEN=your_secure_bearer_token_min_32_chars

# Instagram Account Credentials
# The service will use these credentials to log into Instagram
# WARNING: Use a dedicated automation account, not your personal account
# Instagram may flag or ban accounts detected doing automated actions
INSTAGRAM_USERNAME=your_instagram_username
INSTAGRAM_PASSWORD=your_instagram_password

# Proxy Configuration (Optional but Recommended)
# Using a proxy helps avoid IP-based rate limiting and geo-blocks
# Format: http://username:password@proxy-host:port (for authenticated proxy)
# Format: http://proxy-host:port (for public proxy)
# Leave blank or comment out if not using a proxy
PROXY_URL=

# ============================================================================
# SECURITY WARNINGS
# ============================================================================
# 1. NEVER expose your .env file publicly
# 2. Use strong, unique credentials for the Instagram account
# 3. Enable IP whitelisting on your firewall if possible
# 4. Rotate BEARER_TOKEN regularly (monthly recommended)
# 5. Monitor Instagram account for unusual activity warnings
# 6. Consider using residential proxies for production to avoid blocks
# ============================================================================

